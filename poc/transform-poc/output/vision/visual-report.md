# Vision Comparison Report

## Summary

Final selected pass: 4 of max 5
Last measured pass: 5
Selected visual score: 16.31
Repair provider: openai
Acceptance gate: <= 4% max mismatch and <= 20px max height delta
Stop reason: max-repair-passes - Reached the configured maximum repair pass count after rejecting regressed pass 5; kept best pass 4.

| Pass | Viewport | Score | Size | Pixel mismatch | Width / height delta | Diff |
| ---: | --- | ---: | ---: | ---: | ---: | --- |
| 0 | desktop | 37.17 | 1440x1200 | 22.92% | 0px / 829px | `vision/pass-0/diff-desktop.png` |
| 0 | mobile | 37.17 | 390x1200 | 28.88% | 0px / 263px | `vision/pass-0/diff-mobile.png` |
| 1 | desktop | 16.33 | 1440x1200 | 2.28% | 0px / 11px | `vision/pass-1/diff-desktop.png` |
| 1 | mobile | 16.33 | 390x1200 | 16.01% | 0px / 32px | `vision/pass-1/diff-mobile.png` |
| 2 | desktop | 50.81 | 1440x1200 | 2.19% | 0px / 11px | `vision/pass-2/diff-desktop.png` |
| 2 | mobile | 50.81 | 390x1200 | 43.71% | 6px / 710px | `vision/pass-2/diff-mobile.png` |
| 3 | desktop | 31.72 | 1440x1200 | 2.16% | 0px / 11px | `vision/pass-3/diff-desktop.png` |
| 3 | mobile | 31.72 | 390x1200 | 27.32% | 0px / 440px | `vision/pass-3/diff-mobile.png` |
| 4 | desktop | 16.31 | 1440x1200 | 2.15% | 0px / 11px | `vision/pass-4/diff-desktop.png` |
| 4 | mobile | 16.31 | 390x1200 | 15.89% | 0px / 42px | `vision/pass-4/diff-mobile.png` |
| 5 | desktop | 80.47 | 1440x1200 | 2.15% | 0px / 11px | `vision/pass-5/diff-desktop.png` |
| 5 | mobile | 80.47 | 390x1200 | 44.61% | 0px / 3586px | `vision/pass-5/diff-mobile.png` |

## Repairs

- after pass 0, repair slot 0, apply `repair-1` (openai-vision-artifact, vision-css addition): The visible failures are caused by selector/WordPress-wrapper mismatch rather than missing editable content. A scoped CSS adapter is the smallest repair that fixes the dominant white-button blobs, desktop grid wrapping, and component-row failures without replacing the whole editable block tree. Focus: open. Real action: Add this stylesheet after the current vision CSS so it overrides WordPress button/column defaults only inside the existing Boogie Bar section classes. Expected effect: Header, CTAs, event cards, drink cards, hero details, cassette, badges, and contact grid should snap back to the source layout contract. The largest mismatch regions and desktop height delta should drop substantially because the event and drinks rows no longer wrap incorrectly and the white default buttons disappear. Editability risk: Low to medium. Text remains editable in core blocks. The only medium-risk compromise is the header brand mark: because the current block stores “BB boogie bar” in one core/button instead of separate brand spans, CSS uses pseudo-elements to recreate the separate BB mark and brand text visually. A later block-tree pass could make that fully semantic if required.
- after pass 1, repair slot 1, apply `repair-2` (openai-vision-artifact, vision-css addition): The block structure is semantically adequate and all visible content is present. The remaining actionable issues are selector/wrapper styling failures on reusable components, especially mobile, so a scoped additive stylesheet is the lowest-risk repair. Focus: styling-refinement. Real action: Add this scoped CSS after the current vision stylesheet and prior repair CSS. Expected effect: Drink price circles move to the correct top-right position, mobile card stacks and contact grid align more consistently, the mobile Visit CTA matches the source pill scale, and minor mobile hero vertical drift is reduced while desktop remains essentially unchanged. Editability risk: Low. This only targets existing classes and WordPress wrappers; all text, links, buttons, and card content remain editable core blocks.
- after pass 1, repair slot 2, apply `repair-2` (openai-vision-artifact, vision-css addition): The remaining drift is caused by WordPress paragraph/column wrappers inside otherwise-correct editable block structure. A small scoped additive CSS repair fixes the visible mobile component scale and badge-placement failures without risking the already-good desktop macro layout. Focus: regression-recovery. Real action: Append this scoped CSS after the existing vision stylesheet and the prior repair CSS. Do not replace the block tree for this pass. Expected effect: Drink price badges move to the source top-right position on every cocktail card; drink card heights and text positions become more source-like on mobile; the visit contact card keeps the compact 2x2 grid rather than drifting through core column behavior. Desktop should remain visually close while mobile mismatch and height delta should improve. Editability risk: Low. The repair only changes presentation of existing editable core paragraph, group, button, and columns blocks. It does not hide content, replace text with generated content, or introduce raw HTML.
- after pass 1, repair slot 3, apply `repair-3-mobile-component-scale-recovery` (openai-vision-artifact, vision-css addition): The page content and major section order are correct, and desktop already passes visually. A full block-tree or full CSS replacement would risk regressing the good desktop state. The high-leverage remaining mobile mismatch is caused by WordPress wrapper/element differences for repeated components and CTAs, so a small scoped additive CSS recovery is appropriate. Focus: regression-recovery. Real action: Append this CSS after the current vision repair stylesheet from the best pass. Do not apply the regressed candidate pass 3 CSS. Expected effect: Drink cards regain the source top-right price badge geometry; the hero action row matches the source mobile two-pill width; the visit enquiry CTA expands to the source full-card mobile button scale while preserving the already-close desktop layout. Editability risk: Low. The repair only changes presentation of existing core Paragraph/Button blocks and does not hide or replace editable text, links, labels, or repeated items.
- after pass 4, repair slot 4, apply `repair-4-mobile-responsive-rhythm-and-scale` (openai-vision-artifact, vision-css addition): The block structure is semantically correct and desktop already passes. The remaining failures are mobile-only cascade/wrapper scale differences, so a scoped additive CSS repair is safer than replacing the block tree or full stylesheet. Focus: styling-refinement. Real action: Append this CSS after all previous vision repair CSS so it wins the mobile breakpoint cascade. Keep it scoped to max-width:600px to avoid regressing the passing desktop viewport. Expected effect: Mobile hero, stacked cards, booth/contact panels, and CTA pills will match the source component scale and vertical rhythm more closely, reducing the high mobile mismatch while leaving desktop unchanged. Editability risk: Low. The repair only styles existing core Group, Columns, Buttons, Heading, and Paragraph blocks; all text, links, and repeated items remain editable in WordPress.

## Repair Tasks

- after pass 0, repair slot 0, `task-1` (high, vision-css-addition): Default WordPress button styling creates large white blobs in the fixed header, hero CTA row, featured event link, and visit CTA. Target: Header .site-header button wrappers; .hero-inner CTA buttons; .feature-event booking link; .visit-card enquiry button.. Fix: Add scoped adapter selectors for .wp-block-buttons and .wp-block-button__link inside the existing Boogie Bar sections. Restyle header first button as the brand pill/mark, header second button group as transparent nav links, hero/visit buttons as neon primary/secondary pills, and featured event booking as a plain acid text link. Verification: Desktop and mobile screenshots should show no white rectangular button blocks. Header should be a compact glass pill, hero buttons should be horizontal neon pills, and the visit/enquire CTA should match the source gradient button.
- after pass 0, repair slot 0, `task-2` (high, vision-css-addition): Desktop repeated-card geometry is wrong: event cards wrap to 2+1 and drinks render as 2x2 instead of a single desktop row. Target: .event-strip.wp-block-columns and .menu-grid.wp-block-columns with their .wp-block-column children.. Fix: Override WordPress column flex defaults with section-scoped CSS grid: three equal event columns on desktop and four equal drink columns on desktop; retain one-column stacking on mobile. Verification: At 1440px the Saturday section has three cards in one row and the drinks section has four cards in one row; page height should reduce significantly toward the source height. At 390px both sections remain stacked vertically.
- after pass 0, repair slot 0, `task-3` (medium, vision-css-addition): Hero party details card does not match source definition-list visual rows because the block tree uses paragraph groups rather than dl/dt/dd. Target: .hero-card-details, .hero-card-detail, .detail-label, .detail-value.. Fix: Style the existing grouped paragraph structure as compact flex label/value rows with top borders, muted labels, bold right-aligned values, and reset paragraph margins. Verification: Hero card should show Doors/Age/Dress rows as horizontal label/value pairs like the mockup, not loose stacked text.
- after pass 0, repair slot 0, `task-4` (medium, vision-css-addition): Decorative and repeated inline elements lose source component scale because generated wrappers/empty groups are not covered. Target: .cassette-window, .track-list p, .contact-grid.wp-block-columns.. Fix: Create cassette reels with pseudo-elements since the rendered empty cassette-window has no span children; style track-list paragraphs as compact badges; force contact-grid columns into a two-column card grid on desktop and mobile where space allows. Verification: Cassette shows two reel circles, soundtrack styles appear as rounded badges, and visit contact details stay in compact boxed grid cells rather than WordPress column spacing.
- after pass 1, repair slot 1, `task-1` (high, vision-css-addition): Drink price badges appear in the body/left side of cocktail cards instead of as small white circles at the top-right corner. Target: Drinks section, all .drink-card items on desktop and mobile.. Fix: Force .drink-card to be a positioning context and pin .drink-price absolutely to top/right with fixed circular dimensions, no paragraph margin, high z-index, dark text, and white background. Verification: Each cocktail card shows exactly one small white price circle in the top-right corner, matching the source; the title text no longer overlaps the price badge.
- after pass 1, repair slot 1, `task-2` (medium, vision-css-addition): Mobile Visit enquiry button is too small/compact compared with the source mobile full-width neon pill. Target: Visit section .visit-card direct .wp-block-buttons button on mobile.. Fix: At mobile width, make the visit-card button wrapper and button flex to 100% width, while preserving the existing gradient button styling. Verification: On 390px viewport, the Enquire now button spans the visit card width like the mockup and remains a single centered pill.
- after pass 1, repair slot 1, `task-3` (medium, vision-css-addition): Mobile repeated card sections are slightly offset by WordPress Columns wrapper behavior and default column flex basis/margins. Target: Mobile .event-strip, .menu-grid, and .contact-grid WordPress column wrappers.. Fix: At mobile width, explicitly convert event and drink Columns to one-column CSS grids, contact-grid to two equal columns, and reset child .wp-block-column margins/flex basis. Verification: On mobile, Saturday cards and drink cards stack as full-width cards with consistent gaps; contact details remain a 2x2 grid matching the source.
- after pass 1, repair slot 1, `task-4` (low, vision-css-addition): Mobile hero/action sizing has minor line-height and button-size drift that propagates vertical mismatch near the top of the page. Target: Mobile hero heading, hero copy, hero buttons, and hero card placement.. Fix: Apply a small mobile-only normalization for hero display, heading line-height, copy spacing, compact action pills, and relative hero card placement without changing desktop layout. Verification: On 390px viewport, the hero title line breaks, button row, party-detail card, and horizon grid align closer to the source without hiding any visible hero elements.
- after pass 1, repair slot 2, `task-1` (high, vision-css-addition): Drink price badges are visibly misplaced as white circles on the left/in the text flow instead of the source top-right badge position. Target: Drinks section, all .drink-card cards, especially mobile and desktop price circles.. Fix: Make .drink-card a positioned container, remove .drink-price from normal paragraph flow, pin .drink-price to top-right, normalize its size, margin, text color, font weight, and z-index. Add explicit h3 top spacing so removing price from flow does not pull drink titles upward. Verification: In desktop and mobile screenshots, each £10/£11/£12/£9 badge appears as a small white circle at the upper-right corner of its cocktail card, with the drink name remaining in the lower half of the card and not overlapping the badge.
- after pass 1, repair slot 2, `task-2` (medium, vision-css-addition): Mobile drink cards are slightly shorter/compressed compared with the source, contributing to the 32px mobile height deficit and text/decorative-line mismatch. Target: Mobile .drinks .menu-grid and .drink-card cards.. Fix: At max-width 700px, force the drinks menu to a one-column grid, set a source-like gap, and give each .drink-card a stable min-height and padding so the decorative cocktail outline, title, body, and badge align at the same scale as the mockup. Verification: On 390px mobile, the four drink cards stack vertically with consistent source-like height, visible spacing between cards, price badges at top-right, and no text collision with the cocktail outline.
- after pass 1, repair slot 2, `task-3` (medium, vision-css-addition): Visit contact details can inherit WordPress core/columns mobile stacking and margins instead of the compact source contact grid. Target: Visit section .contact-grid and its .wp-block-column children.. Fix: Override .visit .contact-grid to remain a compact two-column CSS grid on mobile, remove core column margins/flex widths, and normalize inner paragraph margins so labels/values match the card layout. Verification: On 390px mobile, Phone/Email and Address/Hours render as compact two-column rows inside the visit card, with no unexpected vertical column stacking or default WordPress column gaps.
- after pass 1, repair slot 3, `task-1` (high, vision-css-addition): Drink price bubbles are visibly misplaced on mobile and desktop, appearing near the left edge/middle of the card instead of the top-right corner like the mockup. Target: Drinks section .menu-grid / .drink-card / .drink-price. Fix: Add a scoped override that makes each .drink-card a positioning context and absolutely positions p.drink-price at top-right with fixed circular sizing, removing inherited left/bottom/transform placement. Verification: In all four drink cards, the £10/£11/£12/£9 circles sit in the top-right corner and no price circle overlaps the drink name/body copy.
- after pass 1, repair slot 3, `task-2` (medium, vision-css-addition): Mobile hero CTA pair is too narrow; source shows two balanced horizontal pills spanning most of the hero content width. Target: Mobile .hero-inner > .wp-block-buttons and child core button wrappers. Fix: At mobile widths, force the hero button wrapper to a two-column grid, make each core button/link fill its column, and use compact mobile padding/font sizing so the labels remain on one line. Verification: At 390px width, Reserve a booth and See this Saturday remain side-by-side and together span approximately the same width as the mockup CTA row.
- after pass 1, repair slot 3, `task-3` (medium, vision-css-addition): Mobile visit/enquiry CTA is rendered as a small pill; source shows the enquiry button spanning the contact card width. Target: Mobile .visit-card > .wp-block-buttons / Enquire now button. Fix: At mobile widths, set the visit button wrapper and core button/link to width:100% with the existing gradient button styling preserved. Verification: At 390px width, Enquire now is a full-width pill inside the visit card, aligned with the contact fields above it.
- after pass 4, repair slot 4, `task-4-1` (high, vision-css-addition): Hero mobile stack is visually close but vertically/typographically offset, especially the h1/copy/buttons/card grouping against the source mobile hero. Target: Mobile .hero, .hero-inner, .hero h1, .hero-copy, hero buttons, .hero-card, and decorative orbits/grid.. Fix: Add a max-width:600px override that normalizes hero padding, h1 line-height/width, copy margin, core button wrapper gap/min-height, hero card width/padding, and mobile orbit/grid dimensions without affecting desktop. Verification: At 390px width, the hero kicker/h1/buttons/card should align with the source proportions; hero buttons remain horizontal and the details card stays visible below the buttons.
- after pass 4, repair slot 4, `task-4-2` (high, vision-css-addition): Repeated mobile stacked card sections retain small WP Columns/Column rhythm differences, causing cumulative vertical and text-position drift. Target: Mobile .event-strip and .menu-grid, including .event-card and .drink-card inside .wp-block-columns/.wp-block-column wrappers.. Fix: Force the event and menu grids into single-column block flow at mobile, zero default WP column margins, set source-like gaps, card padding, min-heights, heading line-height, and price badge sizing. Verification: At 390px, Saturday event cards and drink cards stack with the same widths, gaps, and internal text/badge positions as the mockup; no cards become horizontally squeezed or over-tall.
- after pass 4, repair slot 4, `task-4-3` (medium, vision-css-addition): Intro, soundtrack, booth, and visit panels have small mobile panel-height and section-spacing mismatches that accumulate into the remaining height delta. Target: Mobile .intro-panel, .panel-dancefloor, .soundtrack, .record-stack, .booth-frame, .booth-details, .visit-card, .contact-grid, and .map-card.. Fix: Normalize mobile section padding, panel/card border radii, record-stack/map heights, booth detail row heights, and contact-grid item padding using scoped overrides. Verification: At 390px, the intro panel, record stack, booth rows, contact cards, and map occupy the same visual scale as the source and the total rendered height moves within the 20px threshold.
- after pass 4, repair slot 4, `task-4-4` (medium, vision-css-addition): Core button wrappers still introduce inconsistent mobile CTA scale in hero, event, and visit areas. Target: Mobile .wp-block-buttons and .wp-block-button__link within .hero, .feature-event, and .visit-card.. Fix: Set mobile-only button gap, min-height, padding, font-size, and visit CTA width to match the source pill proportions while preserving editable core Button blocks. Verification: Hero CTAs stay side-by-side, the event link is a small text/pill link, and the visit CTA remains a full-width neon pill on mobile.

## Rejected Repairs

- No rejected repairs.

## Regression Recoveries

- pass 2 rejected: score 50.81 from `rendered/iterations/pass-2.html`; rolled back to pass 1 score 16.33 from `rendered/iterations/pass-1.html` (rolled-back-and-focused).
- pass 3 rejected: score 31.72 from `rendered/iterations/pass-3.html`; rolled back to pass 1 score 16.33 from `rendered/iterations/pass-1.html` (rolled-back-and-focused).
- pass 5 rejected: score 80.47 from `rendered/iterations/pass-5.html`; rolled back to pass 4 score 16.31 from `rendered/iterations/pass-4.html` (kept-best-at-pass-limit).

## Final Screenshots

- `desktop`: mockup `vision/mockup-desktop.png`, rendered `vision/rendered-desktop.png`, diff `vision/diff-desktop.png`
- `mobile`: mockup `vision/mockup-mobile.png`, rendered `vision/rendered-mobile.png`, diff `vision/diff-mobile.png`

## Observations

- `desktop` (low): No large visual drift detected by this coarse POC comparator. The transform can continue to semantic/editability checks before a final acceptance decision.
- `mobile` (high): Rendered page height still diverges from the mockup. The transform planner should re-check layout primitives, responsive behavior, and spacing before choosing or styling blocks.

## Comparator Notes

- PNG diff is the score and regression gate.
- LLM vision can regenerate an ordered repair bundle per pass when `POC_VISION_REPAIR_PROVIDER=openai`.
- The final rendered HTML is selected from the best-scoring measured pass, not necessarily the last measured pass.
- Regressed candidates are rejected; the next repair starts from the best measured state with instructions to choose a different high-leverage bundle.
- Full-page screenshots are captured with Playwright.
- Animations and transitions are disabled before capture to reduce noisy marquee diffs.
- Pixelmatch compares the shared cropped area and reports page-size deltas separately.
- A pass is accepted when the maximum viewport mismatch percentage and maximum viewport height delta are both within the configured gate.
- This POC runs up to 5 repair passes.
